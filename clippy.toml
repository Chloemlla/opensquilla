# OpenSquilla Clippy Configuration
# See https://doc.rust-lang.org/clippy/ for all available lints
#
# Only configuration keys are valid here (e.g. `msrv`). Lint *levels*
# (allow/deny/warn) live in Cargo.toml [lints] tables or on the clippy
# command line; the rust-ci.yml clippy job carries the project's allows.

# MSRV: we track stable Rust
msrv = "1.85"
